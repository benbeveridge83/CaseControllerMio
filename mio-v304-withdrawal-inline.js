import fs from 'node:fs'
const app=fs.readFileSync(new URL('./src/mioWithdrawalInlineApp.inc',import.meta.url),'utf8')
function once(code,from,to,label){const i=code.indexOf(from);if(i<0||code.indexOf(from,i+from.length)>=0)throw new Error('V304 integration anchor changed: '+label);return code.replace(from,to)}
export default function mioV304WithdrawalInline(){return {name:'mio-v304-withdrawal-inline',enforce:'pre',transform(source,id){
 const path=id.split('?')[0].replaceAll('\\','/');let code=source
 if(path.endsWith('/src/mioWithdrawalRepository.js'))return {code:once(code,"from './mioWithdrawalWorkflow.js'","from './mioWithdrawalWorkspaceState.js'",'workspace events'),map:null}
 if(path.endsWith('/src/App.jsx')){
  code=once(code,"const MIO_APP_VERSION = 'Mio V303 (withdrawal next-action dashboard)'","const MIO_APP_VERSION = 'Mio V304 (inline withdrawal workspace)'",'version')
  code=once(code,"  const [mioWithdrawalIntegrationError,setMioWithdrawalIntegrationError]=useState('')","  const [mioWithdrawalIntegrationError,setMioWithdrawalIntegrationError]=useState('')\n  const [mioWdDraftRequest,setMioWdDraftRequest]=useState(null),[mioWdFocusMatter,setMioWdFocusMatter]=useState('')",'draft request')
  code=once(code,'  function renderWithdrawalsPage() {',app+'\n  function renderWithdrawalsPage() {','adapters')
  code=once(code,"if(!matter||!state||state.status!=='active')throw new Error('Open an active withdrawal first.')","if(!matter||!state||state.status!=='active'||state.paused)throw new Error('Open an active, unpaused withdrawal first.')\n    setMioWdFocusMatter(String(matterId))",'paused module')
  code=once(code,"const mapped=draftingProfile.withdrawal_templates?.[step.id==='notice'?'notice':'motion']","const mapped=definition.template_id||state.steps[step.id]?.template_id||draftingProfile.withdrawal_templates?.[step.id==='notice'?'notice':'motion']",'linked template')
  code=once(code,"setDraftingSelection(current=>({...current,field_values:{...current.field_values,withdrawal_step_id:step.id}}));setPage('drafting');return","setDraftingSelection(current=>({...current,field_values:{...current.field_values,withdrawal_step_id:step.id}}));if(definition.autoGenerate)setMioWdDraftRequest({matter_id:String(matterId),template_id:String(template.id)});setPage('drafting');return",'generate after selection')
  code=once(code,"setEfileAgentDraft({...defaultMioEfileDraft(),matter_id:matterId,cause_number:matter.cause_number||'',case_style:matter.name||'',court_name:matter.courts?.court_name||'',withdrawal_step_id:step.id})\n      setEfileAgentSelectedJobId('');setPage('efile');return","await mioWdPrepareEfile(matterId,step,state,definition.document_id||state.steps[step.id]?.document_ids?.[0]);return",'efile PDF connection')
  code=once(code,'settings={renderMioDraftingDefaults()} legacy={renderWithdrawalsPageLegacy()}','settings={renderMioDraftingDefaults()} legacy={null} templates={draftingTemplates} profile={draftingProfile} timeEntries={billingEntries} onAddTime={(matterId,step)=>openBillingWindow({matter_id:matterId,matter_step:"Withdrawal - "+step.name,description:step.name})} financeRows={mioWdFinanceRows()} renderTrustGraph={mioWdRenderGraph} onEnter={mioWdEnter} onRefreshFinance={refreshMioFinancialGraphData} initialExpanded={mioWdFocusMatter} agentConnected={efileAgentStatus.connected}', 'workspace props')
  code=once(code,"graph_event_kind: 'invoice_sent', graph_event_label: `${marker.invoice_number || 'Invoice'} sent`, graph_event_source: 'Mio invoice email history'","graph_event_kind: marker.graph_event_kind || 'invoice_sent', graph_event_label: marker.label || `${marker.invoice_number || 'Invoice'} sent`, graph_event_source: marker.graph_event_source || 'Mio invoice email history'",'notice marker provenance')
  const draftHeader='  function renderDraftingPage(options = {}) {'
  // The existing wrapper remains in charge of template readiness and document formatting.
  code=once(code,draftHeader,"  function renderDraftingPageV304Inner(options = {}) {",'draft wrapper rename')
  code=once(code,'  function renderWithdrawalsPage() {',"  function renderDraftingPage(options = {}) {return <>{draftingSelection.field_values?.withdrawal_step_id&&<div className=\"mio-wd-return\"><strong>Linked withdrawal draft</strong><span>Generate, review and save the document to this matter, then return to approve the step.</span><button type=\"button\" onClick={()=>setPage('withdrawals')}>Return to withdrawal row</button></div>}{renderDraftingPageV304Inner(options)}</>}\n  function renderWithdrawalsPage() {",'return to withdrawal')
  return {code,map:null}
 }
 if(!path.endsWith('/src/MioWithdrawalDashboard.jsx'))return null
 code="import {workflowAttention,sortWithdrawalRows,completionLabels,moduleLabels,canCompleteWithdrawal,nextPrompt,withdrawalTimeEntries} from './mioWithdrawalWorkspaceState.js'\nimport WithdrawalTrustPanel from './MioWithdrawalTrustPanel.jsx'\nimport './mioWithdrawalInline.css'\n"+code
 code=once(code,'WITHDRAWAL_STEPS,STATUS_LABELS,workflowAttention,sortWithdrawalRows,ageLabel','WITHDRAWAL_STEPS,STATUS_LABELS,ageLabel','workspace attention')
 code=once(code,'settings,legacy})','settings,legacy,templates=[],profile={},timeEntries=[],onAddTime,financeRows=[],renderTrustGraph,onEnter,onRefreshFinance,initialExpanded="",agentConnected=false})','props')
 code=once(code,"[expanded,setExpanded]=useState('')","[expanded,setExpanded]=useState(initialExpanded)",'restore row')
 code=once(code,' const bottom=useRef(null)',' const bottom=useRef(null)\n const[showAllSteps,setShowAllSteps]=useState(false)\n useEffect(()=>{if(initialExpanded)setExpanded(initialExpanded)},[initialExpanded])','all steps toggle')
 code=once(code,' const counts={active:0,needs:0,waiting:0,complete:0}',' const counts={active:0,needs:0,waiting:0,complete:0,paused:0}','counts')
 code=once(code,"counts[a.needsMe?'needs':'waiting']++","counts[a.kind==='paused'?'paused':a.needsMe?'needs':'waiting']++",'paused count')
 code=once(code,"filter==='complete'&&a.kind==='complete'","filter==='paused'&&a.kind==='paused'||filter==='complete'&&a.kind==='complete'",'paused filter')
 code=once(code,"['complete','Completed'],['all','All including completed']","['paused','Paused'],['complete','Completed'],['all','All including completed']",'paused option')
 code=once(code,' async function save(){',` const completeStep=(row,d)=>{edit(row,d);setEditor(current=>({...current,title:completionLabels[d.id]||d.name,status:'complete',note:completionLabels[d.id]||d.name,reference:row.state.steps[d.id].document_ids?.join(', ')||row.state.steps[d.id].evidence?.reference||(d.id==='decision'?'Attorney withdrawal approval':''),confirmed:false}))}
 const addNote=(row,d)=>setEditor({type:'note',title:'Add note - '+d.name,matter:row.matter_id,id:d.id,note:''})
 async function save(){`,'step helpers')
 code=once(code,'  let e\n',"  let e\n  if(editor.type==='note')e={type:'step_note',step_id:editor.id,note:editor.note}\n",'note save')
 code=once(code,"onClick={()=>{setSettingsOpen(!settingsOpen);if(!settingsOpen)setTimeout(()=>bottom.current?.scrollIntoView({behavior:'smooth'}),0)}}","onClick={()=>setSettingsOpen(!settingsOpen)}",'no settings scroll')
 code=once(code,' <div className="mio-wd-counts">',' {renderTrustGraph&&<WithdrawalTrustPanel rows={financeRows} renderGraph={renderTrustGraph} onEnter={onEnter} onRefresh={onRefreshFinance}/>}\n {settingsOpen&&<section className="mio-wd-detail"><h2>Withdrawal and drafting settings</h2>{settings}</section>}\n <div className="mio-wd-counts">','trust panel')
 // Reuse all existing evidence, history and closeout dialogs, but put the detail inside the clicked row.
 const detailStart=code.indexOf(' {selected&&<section className="mio-wd-detail">'),detailEnd=code.indexOf('\n <div ref={bottom}>',detailStart)
 if(detailStart<0||detailEnd<0)throw new Error('V304 detail anchors changed')
 let detail=code.slice(detailStart,detailEnd)
 code=code.slice(0,detailStart)+code.slice(detailEnd)
 detail=detail.replace('{selected&&<section','{selected?.matter_id===r.matter_id&&<section')
 detail=once(detail,'<h2>{selected.name}</h2>','<h2>{selected.client} - {selected.name}</h2><span className="mio-wd-row-label">Whole withdrawal</span>{selected.state?.status!=="complete"&&<><button type="button" disabled={busy||!selected.state} onClick={()=>run(()=>store.apply(owner,selected.matter_id,{type:"workspace_pause",paused:!selected.state.paused}))}>{selected.state?.paused?"Resume":"Pause"}</button><button type="button" disabled={busy||!canCompleteWithdrawal(selected.state)} title="Finish signed order, case-status update, client delivery and remaining reviews first." onClick={()=>completeStep(selected,WITHDRAWAL_STEPS.find(s=>s.id==="close_workflow"))}>Mark complete</button></>}','row controls')
 detail=once(detail,'<div className="mio-wd-lanes">','<div className="mio-wd-tools"><button type="button" aria-pressed={showAllSteps} onClick={()=>setShowAllSteps(!showAllSteps)}>{showAllSteps?"Show current steps only":"Show all steps / completed work"}</button>{selected.state.paused&&<strong>Paused - resume to continue steps.</strong>}</div><details className="mio-wd-time"><summary>All withdrawal time entries ({withdrawalTimeEntries(timeEntries,selected.matter_id).length})</summary><table><thead><tr><th>Date</th><th>Step / description</th><th>Hours</th><th>Amount</th></tr></thead><tbody>{withdrawalTimeEntries(timeEntries,selected.matter_id).map(e=><tr key={e.id}><td>{e.date}</td><td>{e.matter_step}<div>{e.description}</div></td><td>{e.billing_time}</td><td>{Number(e.amount||0).toLocaleString(undefined,{style:"currency",currency:"USD"})}{(e.non_billable||e.do_not_bill)&&" (not billed)"}</td></tr>)}</tbody></table>{!withdrawalTimeEntries(timeEntries,selected.matter_id).length&&<p>No withdrawal time recorded. Use Add time on a step.</p>}</details><div className="mio-wd-lanes">','time view')
 detail=once(detail,'WITHDRAWAL_STEPS.map(s=>s.lane)','WITHDRAWAL_STEPS.filter(s=>showAllSteps||["needs_action","needs_approval","waiting"].includes(selected.state.steps[s.id].status)).map(s=>s.lane)','visible lanes')
 detail=once(detail,'WITHDRAWAL_STEPS.filter(s=>s.lane===lane)','WITHDRAWAL_STEPS.filter(s=>s.lane===lane&&(showAllSteps||["needs_action","needs_approval","waiting"].includes(selected.state.steps[s.id].status)))','visible steps')
 detail=once(detail,'<strong>{d.name}</strong>','<strong>{WITHDRAWAL_STEPS.indexOf(d)+1}. {d.name}</strong>{nextPrompt(selected.state,d.id)&&!["complete","cancelled","not_started"].includes(t.status)&&<p className="mio-wd-prompt">{nextPrompt(selected.state,d.id)}</p>}','next prompt')
 detail=once(detail,'{selected.state.status!==\'complete\'&&<div className="mio-wd-tools">',`{['drafting','notice'].includes(d.id)&&<div className="mio-wd-links"><label>Connected drafting template<select value={t.template_id||profile.withdrawal_templates?.[d.id==='notice'?'notice':'motion']||''} disabled={busy||selected.state.paused||selected.state.status==='complete'||['complete','cancelled'].includes(t.status)} onChange={e=>run(()=>store.apply(owner,selected.matter_id,{type:'template_link',step_id:d.id,template_id:e.target.value}))}><option value="">Use configured / built-in template</option>{templates.filter(x=>x.is_active!==false&&x.status!=='retired').map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Saved documents to review (select one or more)<select multiple value={t.document_ids||[]} disabled={busy||selected.state.paused||selected.state.status==='complete'||['complete','cancelled'].includes(t.status)} onChange={e=>run(()=>store.apply(owner,selected.matter_id,{type:'document_link',step_id:d.id,document_ids:Array.from(e.target.selectedOptions,x=>x.value)}))}>{documents.filter(x=>String(x.matter_id)===selected.matter_id).map(x=><option key={x.id} value={x.id}>{x.file_name||x.name}</option>)}</select></label>{(t.document_ids||[]).map(id=>{const doc=documents.find(x=>String(x.id)===id&&String(x.matter_id)===selected.matter_id);return doc&&<button type="button" key={id} onClick={()=>onViewDocument(doc)}>Review {doc.file_name||doc.name}</button>})}</div>}
 {d.id==='filing'&&<div className="mio-wd-links"><label>Reviewed PDF for e-filing<select value={t.document_ids?.[0]||''} disabled={busy||selected.state.paused||['complete','cancelled'].includes(t.status)} onChange={e=>run(()=>store.apply(owner,selected.matter_id,{type:'document_link',step_id:d.id,document_ids:e.target.value?[e.target.value]:[]}))}><option value="">Select a saved matter PDF</option>{documents.filter(x=>String(x.matter_id)===selected.matter_id&&(/\\.pdf$/i.test(x.file_name||x.name||'')||x.file_type==='application/pdf')).map(x=><option key={x.id} value={x.id}>{x.file_name||x.name}</option>)}</select></label><small>{agentConnected?'eFile agent connected.':'eFile agent not confirmed connected - open eFile to connect the local companion.'} Word drafts must be saved as reviewed PDFs before filing.</small></div>}
 {(selected.state.workspace_notes||[]).filter(n=>n.step_id===d.id).map((n,i)=><p key={i} className="mio-wd-note"><small>{when(n.created_at)}</small>{n.note}</p>)}
 {selected.state.status!=='complete'&&<div className="mio-wd-tools">`,'step links')
 detail=once(detail,'<button type="button" disabled={busy} onClick={()=>edit(selected,d)}>Record update / evidence</button>',`<span className="mio-wd-row-label">This step</span><button type="button" disabled={busy} onClick={()=>onAddTime(selected.matter_id,d)}>Add time</button><button type="button" disabled={busy} onClick={()=>addNote(selected,d)}>Add note</button>{!['complete','cancelled'].includes(t.status)&&d.module!=='status'&&<button type="button" disabled={busy||selected.state.paused||(['drafting','notice'].includes(d.id)&&!t.document_ids?.length)} onClick={()=>completeStep(selected,d)}>{completionLabels[d.id]||'Confirm completion'}</button>}<button type="button" disabled={busy||selected.state.paused} onClick={()=>edit(selected,d)}>Waiting / correct status</button>`,'step controls')
 detail=once(detail,"disabled={busy||['not_started','cancelled'].includes(t.status)} onClick={()=>run(()=>onOpen(selected.matter_id,d,selected.state))}>Open {d.module==='email'?'Email':d.module}","disabled={busy||selected.state.paused||['not_started','cancelled','complete'].includes(t.status)||(d.module==='efile'&&!t.document_ids?.length)} onClick={()=>run(()=>onOpen(selected.matter_id,{...d,autoGenerate:d.module==='drafting',template_id:t.template_id,document_id:t.document_ids?.[0]},selected.state))}>{moduleLabels[d.module]||('Open '+d.module)}",'module actions')
 detail=detail.replaceAll("disabled={busy||selected.state.steps.signed_order", "disabled={busy||selected.state.paused||selected.state.steps.signed_order")
 const tableStart=code.indexOf(' <div className="mio-wd-table">'),tableEnd=code.indexOf('\n <p className="mio-wd-muted">',tableStart)
 if(tableStart<0||tableEnd<0)throw new Error('V304 table anchors changed')
 const table=` <div className="mio-wd-table"><table><thead><tr>{['Matter / client','Status','Next action','Action pending','In withdrawal',''].map((t,i)=><th key={i}>{t}</th>)}</tr></thead><tbody>{visible.map(r=>{const a=workflowAttention(r.state,now),p=mioWithdrawalProgressV303(r.state,WITHDRAWAL_STEPS);return <React.Fragment key={r.matter_id}><tr className={'mio-wd-summary '+(a.needsMe?'needs-me':'')} onClick={()=>setExpanded(expanded===r.matter_id?'':r.matter_id)}><td><strong>{r.client||r.name}</strong><div>{r.name} <small>{r.cause_number}</small></div></td><td><b className={'mio-wd-badge '+(a.needsMe?'needs':'')}>{a.kind==='complete'?'COMPLETE':a.kind==='paused'?'PAUSED':a.needsMe?'NEEDS ME':'WAITING'}</b><small>{p.done}/{p.total} steps</small></td><td><strong>{a.step_id==='drafting'?(r.state?.steps.drafting.document_ids?.length?'Review and approve saved motion / order':'Draft the motion / order using the connected template'):a.next}</strong>{a.waiting.length>0&&<small>Waiting on: {a.waiting.join('; ')}</small>}{a.overdue&&<small>Follow-up due</small>}</td><td>{a.needsMe?ageLabel(a.since,now):'-'}</td><td>{ageLabel(r.state?.started_at||r.started_at,now)}</td><td><button type="button" aria-expanded={expanded===r.matter_id} aria-controls={'withdrawal-detail-'+r.matter_id} onClick={e=>{e.stopPropagation();setExpanded(expanded===r.matter_id?'':r.matter_id)}}>{expanded===r.matter_id?'Collapse':'Review'}</button></td></tr>{expanded===r.matter_id&&<tr id={'withdrawal-detail-'+r.matter_id} className="mio-wd-expanded"><td colSpan={6}>${detail}</td></tr>}</React.Fragment>})}{!visible.length&&<tr><td colSpan={6}>No withdrawals match this view.</td></tr>}</tbody></table></div>`
 code=code.slice(0,tableStart)+table+code.slice(tableEnd)
 const oldSettingsStart=code.indexOf(' <div ref={bottom}>'),oldSettingsEnd=code.indexOf('\n {editor&&<Dialog',oldSettingsStart)
 code=code.slice(0,oldSettingsStart)+' <div ref={bottom}/>'+code.slice(oldSettingsEnd)
 code=once(code,"{editor.type==='date'&&", "{editor.type==='note'&&<label>Step note<textarea rows={5} maxLength={4000} value={editor.note} onChange={e=>change({note:e.target.value})}/></label>}\n {editor.type==='date'&&",'note editor')
 code=once(code,"{busy?'Saving...':'Save to workflow'}","{busy?'Saving...':editor.type==='step'&&editor.status==='complete'?(completionLabels[editor.id]||'Confirm completion'):editor.type==='note'?'Save note':'Save to workflow'}",'specific confirmation')
 return {code,map:null}
}}}
